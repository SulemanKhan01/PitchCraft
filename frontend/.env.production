# Production environment variables for Vite build
# VITE_API_BASE is empty — means relative URLs (/api/...)
# Nginx will route /api/* to the FastAPI backend on port 8000

VITE_API_BASE=

# Clerk Authentication — use your PRODUCTION Clerk publishable key
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
